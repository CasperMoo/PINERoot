# 前端开发文档导航

> 📍 这是前端开发的入口文档，帮助你快速找到需要阅读的规范。

## 📖 何时读取哪些文档

### 🎯 开始任何前端开发前

**必读**：
1. 📘 `.rules/PROJECT_OVERVIEW.md` - 了解项目整体架构
2. 📄 本文档 - 确定要读哪些子文档

### 📋 根据任务类型选择文档

| 任务类型 | 必读文档 | 说明 |
|---------|---------|------|
| 🔍 查看已完成模块 | `MODULES.md` | 了解哪些页面/组件禁止修改 |
| 💻 添加新页面/组件 | `MODULES.md` + `DEVELOPMENT.md` | 查看模块清单 + 开发规范 |
| 🎨 UI 设计相关 | `UI_DESIGN.md` | 响应式、主题色、动效规范 |
| 🚀 部署相关 | `DEPLOYMENT.md` | 构建、Nginx 配置 |
| 🤖 Git 提交 | `../AI_COLLABORATION.md` | Git 工作流和协作规范 |

## 📚 文档列表

### MODULES.md - 已完成模块清单 🔒

**内容**：所有已完成并部署的页面和组件，**禁止修改**
- ✅ API 封装层（request.ts, auth.ts, image.ts 等）
- ✅ 状态管理（Zustand - useAuthStore）
- ✅ 路由配置（React Router v7）
- ✅ 路由守卫（PrivateRoute, AdminRoute）
- ✅ 用户认证页面（登录、注册）
- ✅ 管理后台（图片管理）

**何时阅读**：
- 开始任何开发前，先确认不会误改已完成模块
- 需要调用已有 API 或组件时
- 了解项目当前进度

### DEVELOPMENT.md - 开发规范

**内容**：日常开发的代码规范和最佳实践
- 文件组织规范（目录结构、命名规范）
- 代码规范（TypeScript、组件规范）
- API 调用规范（如何使用封装的 request）
- 状态管理规范（Zustand 使用）
- 路由规范（命名、守卫使用）

**何时阅读**：
- 添加新页面或组件
- 不确定代码应该放在哪个目录
- 需要调用 API 或管理状态
- 配置新路由

### UI_DESIGN.md - UI 设计规范

**内容**：UI 设计相关的规范和最佳实践
- 响应式设计规范（移动/平板/PC 断点）
- 样式规范（Tailwind CSS v4 使用）
- 主题色系统（品牌色、配色）
- 性能优化（懒加载、按需导入）

**何时阅读**：
- 设计新页面的 UI
- 需要适配移动端
- 调整主题色或样式
- 优化页面性能

### DEPLOYMENT.md - 部署规范

**内容**：前端构建和部署流程
- 构建命令
- 部署路径（1Panel）
- Nginx 配置要求
- 环境变量管理

**何时阅读**：
- 准备部署到生产环境
- 修改构建配置
- 添加新的环境变量
- 部署相关问题排查

## 🚨 重要提醒

### 每次开发前的检查清单

- [ ] 我读了 `PROJECT_OVERVIEW.md`（了解项目整体）
- [ ] 我读了 `MODULES.md`（知道哪些模块禁止修改）
- [ ] 我知道本次任务要读哪些规范文档
- [ ] 我会用 Git 验证改动范围
- [ ] 我会测试响应式布局（移动/平板/PC）

### 特别注意

1. **禁止修改已完成模块**：除非得到明确确认
2. **响应式设计**：所有页面必须支持移动/平板/PC
3. **使用 Tailwind CSS v4**：在 CSS 中使用 `@import "tailwindcss"`
4. **类型安全**：Axios 类型必须使用 `type` 关键字导入
5. **创建新文件优先**：避免修改已有文件

### 标准开发流程

```
1. 阅读文档（本文档 + 对应规范文档）
   ↓
2. 创建新文件（页面/组件）
   ↓
3. 编写代码（遵循规范）
   ↓
4. 测试响应式（移动/平板/PC）
   ↓
5. Git 验证改动范围
   ↓
6. 提交代码
```

## 🎯 技术栈速查

- **框架**: React 18
- **构建工具**: Vite
- **语言**: TypeScript
- **UI 库**: Ant Design 5
- **CSS**: Tailwind CSS v4
- **状态管理**: Zustand
- **路由**: React Router v7
- **HTTP 客户端**: Axios
- **表单**: react-hook-form + zod

## 🔗 相关文档

- 📘 项目整体概述：`.rules/PROJECT_OVERVIEW.md`
- 📗 后端开发规范：`.rules/BACKEND/README.md`
- 🤖 AI 协作规范：`.rules/AI_COLLABORATION.md`

## 快速参考

### 添加新页面

1. 在 `src/pages/` 创建新目录（如 `NewPage/`）
2. 创建 `index.tsx` 和样式文件
3. 在 `src/App.tsx` 注册路由
4. 添加路由守卫（如需要）
5. 测试响应式布局

### 调用后端 API

```typescript
import { authApi } from '@/api/auth'

const handleLogin = async () => {
  const res = await authApi.login({ email, password })
  // res.data 包含业务数据
}
```

### 使用全局状态

```typescript
import { useAuthStore } from '@/store/auth'

const { user, token, logout } = useAuthStore()
```

### 响应式布局

```typescript
<div className="
  px-4 md:px-8 lg:px-12           // 内边距
  text-sm md:text-base lg:text-lg // 字体大小
  grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3  // 栅格布局
">
```
